{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities used by the Paperloom main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "sql:default",
    "sql:allow-execute"
  ]
}

